{
  "name": "cardume-app",
  "version": "0.0.1",
  "description": "Shell desktop (Tauri v2, Mac-first) do Cardume — visor ao vivo do state.sqlite.",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
